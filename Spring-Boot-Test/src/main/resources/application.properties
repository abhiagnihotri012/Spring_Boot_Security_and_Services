#Changing  port number
server.port=8085

spring.application.name=MySpringBootApp

#loggin level debug for checking how spring is configuring whole application
#logging.level.org.springframework=debug

#Database properties
spring.datasource.url = jdbc:mysql://localhost:3306/springboot?useSSL=false
spring.datasource.username = root
spring.datasource.password= ac3r

#SQL dialect makes JPA generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

#Update the schema whenver required
spring.jpa.hibernate.ddl-auto= update

#Set to only warnings (info will be not shown)
logging.level.root=INFO
#WARN

#Spring basic security credentials
spring.security.user.name=user
spring.security.user.password=u123







